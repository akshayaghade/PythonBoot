#Creating a python module with list and import the module in another .py file and change the value in list

pip install module

import math
print("The value of pi is", math.pi)

pip install pandas

import pandas

#Import a module that picks number and write a program to fetch a random number from 1 to 100 onn every run.

import numpy as np
import random
print(random.randint(0,9))

print(random.randint(50,100))

#Import sys packges and find the python path

import sys
sys.path

sys.version

#Try to install a package and uninstall a packge using pip

pip install flask

pip uninstall flask
